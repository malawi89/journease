<% content_for :meta_title, "Add a Journey" %>
<div class="minheight">
  <div class="container">
    <div class="row">
      <div class="form-stuff">
       <h3 class = "text-center">Add a journey to your trip</h3>

      <% if @journey.errors.any? %>
        <ul>
          <% @journey.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      <% end %>

      <%= render 'form', journey: @journey %>
      </div>
    </div>
  </div>
</div>
